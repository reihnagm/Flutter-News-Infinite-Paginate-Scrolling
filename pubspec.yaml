name: flutter_pagination
description: Flutter Pagination.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # D
  dio: ^4.0.4

  # F
  flutter_spinkit: ^5.1.0

  # I
  infinite_scroll_pagination: ^3.1.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true